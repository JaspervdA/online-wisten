// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyA6M5TRkDA_z6o_R_1NfmRE57VeNOVthJA",
    authDomain: "online-wisten.firebaseapp.com",
    databaseURL: "https://online-wisten.firebaseio.com",
    projectId: "online-wisten",
    storageBucket: "online-wisten.appspot.com",
    messagingSenderId: "849928184097",
    appId: "1:849928184097:web:556fa9671d34577de19e60"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
